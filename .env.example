# Drizzle
# Replace with your DB credentials!
DATABASE_URL="postgres://user:password@host:port/db-name"

# Stack Auth
PUBLIC_STACK_PROJECT_ID=<your-project-id>
PUBLIC_STACK_PUBLISHABLE_CLIENT_KEY=<your-publishable-client-key>
STACK_SECRET_SERVER_KEY=<your-secret-server-key>

# App URL (optional - auto-detected from request if not set)
# Set this only if you need to override the auto-detected origin
# ORIGIN=https://your-domain.com

